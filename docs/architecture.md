# Nova AI Architecture (MVP)

- **Backend**: FastAPI + SQLAlchemy with JWT RBAC and PostgreSQL
- **ML**: XGBoost risk model with SHAP top-factor explanations
- **Frontend**: React + Vite + Tailwind + Recharts
- **Infra**: Docker Compose for local dev, GitHub Actions for CI
